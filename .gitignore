.DS_Store
__pycache__/
*.pyc
.venv/

# Generated by setuptools on every `pip install -e .`. Tracked, it churned the
# git_dirty provenance flag on reinstall with no semantic change (see
# docs/provenance.md); files stay on disk for the editable install.
cwatlas_collector.egg-info/

# This station: LAN addresses, antenna location, data paths. Never tracked.
# See config.example.toml for the shape.
config.toml

# Build artifacts
build/
dist/
